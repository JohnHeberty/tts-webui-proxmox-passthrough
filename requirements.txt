# === CORE WEB FRAMEWORK (Optional - if using FastAPI) ===
# Note: This project can run standalone with Gradio (app_gradio.py or app_standalone.py)
# or with FastAPI integration. FastAPI dependencies are optional.
fastapi==0.120.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# === CORE DEPENDENCIES ===
pydantic>=2.7.0,<2.11.0
pydantic-settings==2.11.0

# === REDIS & CELERY (Optional - for job queue) ===
celery==5.3.4
redis==5.0.1

# === AUDIO PROCESSING CORE ===
# torch e torchaudio são instalados no Dockerfile com versão específica cu118
numpy>=1.26.0,<1.27.0
soundfile==0.12.1
pydub>=0.25.1

# === XTTS (Coqui TTS - ONLY TTS ENGINE) ===
coqui-tts>=0.27.0

# === UTILITIES ===
httpx==0.27.0

# === GRADIO WEBUI ===
gradio>=4.0.0
