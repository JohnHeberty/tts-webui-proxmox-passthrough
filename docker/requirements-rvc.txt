# RVC Dependencies - Sprint 2
# Installed after XTTS stack (requirements.txt)
# Pin versions to avoid conflicts

# === RVC CORE ===
tts-with-rvc==0.1.9

# === FAIRSEQ (Hubert Model) ===
# fairseq==0.12.2 não está disponível no PyPI padrão
# Usar fork compatível ou instalar direto do GitHub
# Por enquanto, vamos tentar a versão mais recente estável
fairseq>=0.12.0

# === FAISS (Index Retrieval) ===
# faiss-gpu para CUDA, faiss-cpu para CPU
# Versão compatível com CUDA 12.1
faiss-gpu==1.7.2; sys_platform != 'win32'
faiss-cpu==1.7.2; sys_platform == 'win32'

# === AUDIO PROCESSING ===
librosa==0.10.1
praat-parselmouth==0.4.3
torchcrepe==0.0.23

# === PITCH EXTRACTION ===
# RMVPE já vem com tts-with-rvc
# Outras dependências opcionais
scipy>=1.11.4  # Requerido por librosa
resampy>=0.4.2  # Requerido por librosa

# === UTILITIES ===
# Garantir compatibilidade com versões do XTTS
numpy>=1.26.0,<2.0.0  # Pin major version
