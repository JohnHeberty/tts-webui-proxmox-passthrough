# Dataset Pipeline Configuration
# =================================
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Transcrição - Paralelização
# ----------------------------
# Número de workers paralelos para Whisper (None = auto-detect baseado em VRAM)
# Whisper base: ~2GB VRAM por worker
# Whisper medium: ~5GB VRAM por worker
# Exemplo: RTX 3090 24GB = 4-6 workers base, 2-3 workers medium
WHISPER_NUM_WORKERS=4

# VRAM mínima livre para iniciar workers (MB)
# Deixa margem de segurança para evitar OOM
WHISPER_MIN_FREE_VRAM_MB=2048

# Batch size para processamento (segmentos por worker)
WHISPER_BATCH_SIZE=1

# Device (cuda, cuda:0, cuda:1, cpu)
WHISPER_DEVICE=cuda

# Audio Processing - Paralelização
# ---------------------------------
# Workers para segmentação VAD (CPU-bound)
VAD_NUM_WORKERS=4

# Training
# --------
# Batch size para training
TRAIN_BATCH_SIZE=4

# Gradient accumulation steps
TRAIN_GRADIENT_ACCUMULATION=2

# Mixed precision (fp16, bf16, fp32)
TRAIN_MIXED_PRECISION=fp16

# Logging
# -------
# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Save logs to file
LOG_TO_FILE=true

# Paths (relative to train/)
# ---------------------------
DATA_DIR=data
OUTPUT_DIR=output
LOGS_DIR=logs
MODELS_DIR=models
